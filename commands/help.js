

function help(){
    console.log( `
    list of commands
       tree -> directorypath
       organize -> directorypath
       help -> lists all the commands
     `);
}

module.exports = {
    helpKey : help
}
